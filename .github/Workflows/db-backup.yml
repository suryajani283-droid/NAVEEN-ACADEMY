name: Daily Database Backup

on:
  schedule:
    - cron: '0 20 * * *'   # 8 PM UTC (2 AM IST) every day
  workflow_dispatch:        # allows manual trigger from GitHub

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          ref: db-backups   # we'll use a separate branch for backups

      - name: Setup PostgreSQL client
        run: sudo apt-get install -y postgresql-client

      - name: Create backups directory
        run: mkdir -p backups

      - name: Dump database
        env:
          PGPASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
        run: |
          pg_dump \
            -h ${{ secrets.SUPABASE_DB_HOST }} \
            -U postgres \
            -d postgres \
            --no-owner \
            --clean \
            --if-exists \
            > backups/db-backup-$(date +'%Y-%m-%d').sql

      - name: Commit and push backup
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add backups/
          git commit -m "Database backup $(date +'%Y-%m-%d')" || echo "No changes to commit"
          git push origin db-backups --force